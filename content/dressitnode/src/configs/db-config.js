const config = {
    user        : "sa", // Database username
    password    : "Lmsaccal2286!", // Database password
    server      : "localhost", // Server IP address
    database    : "DressIt", 
    port        : 1533,
    options: {
        encrypt: false,
        trustServerCertificate: true,
        }
}

export default config;